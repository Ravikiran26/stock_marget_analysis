# QA Suite — deployment config
# Fill in your actual deployed URLs. Used by smoke tests and contract checks.

# Your Railway backend URL (e.g. https://tradfy-backend.up.railway.app)
PRODUCTION_BACKEND_URL=https://web-production-09bcd.up.railway.app

# Your Vercel frontend URL (e.g. https://tradersdiary.in)
PRODUCTION_FRONTEND_URL=https://tradersdiary.in

# Set to "true" if env vars are managed in Vercel + Railway (skip local .env check)
PLATFORM_ENV=true
